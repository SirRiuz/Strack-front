import '../../styles/cards/product.css'

const SkeletonProduct = props => {
  return(
    <div
        className={'product-card dsds'}
        style={{cursor:'auto', background:'purple'}}
    >
    </div>
  )
}

export default SkeletonProduct
